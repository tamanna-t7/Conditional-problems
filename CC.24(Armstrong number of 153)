#include <stdio.h>

int main(){
    int num=153,r,sum=0,A;

    for(A=num;num!=0;num=num/10){
         r=num % 10;
         sum=sum+(r*r*r);
    }
    if(sum==A)

         printf("YES");
    else
         printf("NO");

}
