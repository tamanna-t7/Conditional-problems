#include <stdio.h>

int main()
{
   int i, sum=0;
   printf("Numbers between 1 to 30 and divisible by 3 : \n");
   for(i=1;i<30;i++)
   {
     if(i%3==0)
     {
       printf("%d\n",i);
       sum+=i;
     }
   }
   printf("\nThe sum : %d\n",sum);
}
