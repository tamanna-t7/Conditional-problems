#include<stdio.h>
int main()
{
    int n=10,i,sum=0;

    for(i=2;i<=n;i=i+2)
        sum=sum+i;
    {
    if(sum<30)

    printf("good");

    else if(sum==30)
    printf("Better");

    else if(sum>30)
    printf("best");

    else
        printf("invalid");
    return 0;
   }
}







